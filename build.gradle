    buildscript {
        apply from: "dependencies.gradle"
        repositories {
            google()
            jcenter()
        }
        dependencies {
            classpath "com.android.tools.build:gradle:$ver.androidGradlePlugin"
            classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$ver.kotlinVersion"
            classpath "androidx.navigation:navigation-safe-args-gradle-plugin:$ver.navComponents"
        }
    }

    allprojects {
        repositories {
            google()
            jcenter()
        }
    }

    task clean (type: Delete) {
        delete rootProject.buildDir
    }